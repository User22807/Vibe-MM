# VibeScreener — Native React

This version contains the dashboard markup, styling, rendering logic, state, interactions, simulation, and data directly in `src/App.jsx`. There is no `template.html` or custom HTML template/runtime.

## Run

```bash
npm install
npm run dev
```
